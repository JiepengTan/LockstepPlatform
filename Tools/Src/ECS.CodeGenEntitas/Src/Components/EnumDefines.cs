namespace Lockstep.ECS {
    public enum EAssetID {
        Bullet0
    }

    public enum EDir { }

    public enum ECampType { }
    public enum EItemType {
        Boom,
        AddLife,
        Upgrade,
    }
   
}